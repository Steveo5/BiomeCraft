package steven.dev.quest;

public enum QuestRequirementStatus {
    PROGRESS,
    // Softcomplete means the user has gotten the requirements before but currently they dont have them
//    SOFTCOMPLETE,
    COMPLETE,
    NONE
}
